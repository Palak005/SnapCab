Ride at the snap of a finger.


Ques) When to use app.listen() and when to creat an HTTP server and then using .listen()?
==> When you need WebSockets, custom socket handling, or want to share the server between different services.
    For basic APIs or Express servers where you don’t need low-level control.


Ques) What is the use of CORS?
==> enable web applications to fetch data, use APIs, and integrate with services hosted on different domains. 
    CORS is a security mechanism that works by adding HTTP headers to responses, indicating which origins are permitted to access the resource\



//Auto Suggestions

Using MapMyIndia APi : 
1. Using client id and secret generating the access token
2. By Passing the Auth Token Api call for Autosuggestion, query : address 


//Geocoding
Using Geopify Api for fetching the cooordinates


//Finding the Distance between two locations 



